@page
@model Projet.Pages.Offers.CreateModel

@{
    ViewData["Title"] = "Soumettre une offre";
}

<div class="container mt-4">
    <h2 class="mb-4">📝 Soumission d’une offre</h2>

    <form method="post" class="card p-4 shadow-sm">

        <input type="hidden" name="tenderId" value="@Model.TenderId" />

        <div class="mb-3">
            <label asp-for="Offer.TotalPrice" class="form-label">Montant total</label>
            <input asp-for="Offer.TotalPrice" class="form-control" />
            <span asp-validation-for="Offer.TotalPrice" class="text-danger"></span>
        </div>

        <div class="d-flex justify-content-between">
            <a asp-page="/Tenders/Index" class="btn btn-secondary">
                ⬅ Annuler
            </a>

            <button type="submit" class="btn btn-success">
                ✅ Soumettre
            </button>
        </div>

    </form>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
