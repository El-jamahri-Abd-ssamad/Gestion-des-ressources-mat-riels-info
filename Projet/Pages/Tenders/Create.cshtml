@page
@using Projet.Domain
@model Projet.Pages.Tenders.CreateModel

@{
    ViewData["Title"] = "Créer un appel d'offre";
}

<div class="container mt-4">
    <h2 class="mb-4">📢 Nouvel appel d’offre</h2>

    <form method="post" class="card p-4 shadow-sm">

        <div class="mb-3">
            <label asp-for="Tender.Title" class="form-label">Titre</label>
            <input asp-for="Tender.Title" class="form-control" />
            <span asp-validation-for="Tender.Title" class="text-danger"></span>
        </div>

        <div class="row">
            <div class="col-md-6 mb-3">
                <label asp-for="Tender.StartDate" class="form-label">Date début</label>
                <input asp-for="Tender.StartDate" type="date" class="form-control" />
            </div>

            <div class="col-md-6 mb-3">
                <label asp-for="Tender.EndDate" class="form-label">Date fin</label>
                <input asp-for="Tender.EndDate" type="date" class="form-control" />
            </div>
        </div>

        <div class="d-flex justify-content-between">
            <a asp-page="Index" class="btn btn-secondary">⬅ Retour</a>
            <button type="submit" class="btn btn-primary">✅ Créer</button>
        </div>

    </form>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
