@page
@model Projet.Pages.Tenders.IndexModel

<h2>📢 Appels d’offres</h2>

<table class="table table-bordered">
    <thead>
        <tr>
            <th>Titre</th>
            <th>Période</th>
            <th>Statut</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var t in Model.Tenders)
        {
            <tr>
                <td>@t.Title</td>
                <td>@t.StartDate?.ToShortDateString() - @t.EndDate?.ToShortDateString()</td>
                <td>@t.Status</td>
                <td>
                    <a asp-page="Details" asp-route-id="@t.Id" class="btn btn-primary">
                        Détails
                    </a>
                </td>
            </tr>
        }
    </tbody>
</table>

@if (Model.IsAdmin)
{
    <a asp-page="Create" class="btn btn-success">
        ➕ Nouvel appel d’offre
    </a>

    <a asp-page="/Blacklist/Index" class="btn btn-danger ms-2">
        🚫 Blacklist
    </a>
}
