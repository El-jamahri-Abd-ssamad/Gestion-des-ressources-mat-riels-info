@page
@model Projet.Pages.Tenders.IndexModel

@{
    ViewData["Title"] = "Appels d’offres";
}

<div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center mb-3">
        <h2>📋 Appels d’offres</h2>
        <a asp-page="Create" class="btn btn-success">➕ Nouveau</a>
    </div>

    <table class="table table-bordered table-hover shadow-sm">
        <thead class="table-dark">
            <tr>
                <th>Titre</th>
                <th>Date début</th>
                <th>Date fin</th>
                <th>Statut</th>
                <th>Action</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var t in Model.Tenders)
            {
                <tr>
                    <td>@t.Title</td>
                    <td>@(t.StartDate.HasValue ? t.StartDate.Value.ToShortDateString() : "")</td>
                    <td>@(t.EndDate.HasValue ? t.EndDate.Value.ToShortDateString() : "")</td>
                    <td>
                        <span class="badge bg-info">@t.Status</span>
                    </td>
                    <td>
                        <a asp-page="Details" asp-route-id="@t.Id" class="btn btn-sm btn-primary">
                            🔍 Détails
                        </a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
