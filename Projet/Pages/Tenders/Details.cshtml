@page
@model Projet.Pages.Tenders.DetailsModel

@{
    ViewData["Title"] = "Détails appel d’offre";
}

<div class="container mt-4">
    <h2 class="mb-4">📄 Détails de l’appel d’offre</h2>

    <div class="card shadow-sm p-4 mb-3">
        <p><strong>Titre :</strong> @Model.Tender.Title</p>
        <p><strong>Date début :</strong> @(Model.Tender.StartDate.HasValue ? Model.Tender.StartDate.Value.ToShortDateString() : "")</p>
        <p><strong>Date fin :</strong> @(Model.Tender.EndDate.HasValue ? Model.Tender.EndDate.Value.ToShortDateString() : "")</p>
        <p>
            <strong>Statut :</strong>
            <span class="badge bg-success">@Model.Tender.Status</span>
        </p>
    </div>

    <div class="d-flex justify-content-between">
        <a asp-page="Index" class="btn btn-secondary">⬅ Retour</a>
        <a asp-page="/Offers/Create"
           asp-route-tenderId="@Model.Tender.Id"
           class="btn btn-primary">
            📝 Soumettre une offre
        </a>
    </div>
</div>
